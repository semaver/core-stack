/**
 * enum representing update types for class table\
 * \
 * METADATA_ADDED - type of update if metadata added to the class\
 * METADATA_REMOVED - type of update if metadata removed from class
 *
 * @public
 * @enum
 */
export enum ClassTableUpdateTypes {
    METADATA_ADDED,
    METADATA_REMOVED,
}
